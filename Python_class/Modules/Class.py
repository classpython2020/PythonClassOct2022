#class

#1. lets see how to import the classes from the modules.

    # first we need to import the module.
    # then we have to create a object to store the class  obj=module name.class name
    # after creating the object we can use like this to get the function from the class.
     # obj.function name


import Moduleex

obj=Moduleex.add
obj.sum(10,10)